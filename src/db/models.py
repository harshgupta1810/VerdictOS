"""SQLAlchemy ORM Models.

Defines database tables: deals, findings, debate_transcripts,
escalations. Implements append-only integrity constraints.

Engines: PostgreSQL (production), SQLite (local fallback).
"""
